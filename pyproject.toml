[project]
name = "agentic-migration"
version = "0.1.0"
description = "Deterministik-first Harness: SQL Server -> dbt-Exasol, Triage A/B/C, Qwen als Pruefling."
requires-python = ">=3.11"
dependencies = [
    "sqlglot>=30.0",
]

[project.optional-dependencies]
gate = [
    "sqlfluff>=3.0",
]

[tool.uv]
package = false
